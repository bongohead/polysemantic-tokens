- token: " #"
  meaning_label: "comment_line_indicator"
  text_samples:
    - "Use the marker after the statement, not inside the text you want printed.\n```python\nname = input('name: ')\nprint(name)  # comment after code is ignored\n# print(debug_name)\n```\nIf the editor colors the rest of the line gray, it is a comment. The ad panel starts before the second answer and the code fence is missing its closing ticks."
    - "PY=python3\nWHEELHOUSE=/opt/wheels\ninstall:\n\t$(PY) -m pip install --no-index --find-links=$(WHEELHOUSE) -r requirements.txt\n\t$(PY) -m pip check  # catches stale lockfile on mirror host\nlint:\n\truff check src tests\n\tmypy src  # notebooks handled by separate job\npublish:\n\t$(PY) -m build\n\ttwine check dist/*\n\t# tag push uploads later"
    - "services:\n  worker:\n    image: queue-runner:2026.05\n    command: ./run-worker\n    # ARM64 lane\n    environment:\n      JOB_LIMIT: '80'\n      RETRY_SECONDS: '15'\n  beat:\n    image: queue-runner:2026.05\n    command: ./run-scheduler\n    # Copyright 2026 Queue Ops\n    # ship it, apparently"
    - "Before changing live rows, run the select twice and keep the update commented until the backup has a filename.\n```sql\nSELECT current_date;\n # UPDATE users SET segment = 'dormant';\nSELECT count(*) FROM users;\n```\nBackup checklist: filename, row count, owner initials."
    - "why does this skip the last one??\nfor i in range(5):\n    print(i)  # i thought this stops at five??\n    # sorry copied from a video\nprint('done')\nteacher says the comment is not the problem but I do not get where range ends."
    - "Mina 09:12: can you paste the line that failed?\nOmar 09:13: `podman run ferry refresh  # only works before the API lockout`\nMina 09:13: the comment is fine, the image name is wrong\nOmar 09:14: deeply rude of computers but ok"
    - "commented_roster_export=true\nAsha,08:00,12:00,front desk\nBen,12:00,16:00,phones\n # --- COVERAGE GAP ---\nMara,16:00,20:00,closing\n # MK / 2026-05-04\nTomas,20:00,23:00,inventory\nJules,23:00,02:00,lights"
    - "The cluster handout says to copy only the commands, but the PDF text layer includes the section comment too:\n```\nmodule purge\nmodule load gcc/12 netcdf\n # === REGRID ===\n./bin/regrid input.nc output.nc\n```\nSeveral students pasted the heading into the scheduler form; it did not hurt anything, but the help desk ticket now has three screenshots."
    - "comment_style = 'hash'\nalpha = 0.08\nmax_iter = 900\nseed = 1184\n # lowering alpha hides the wiggle but slows convergence\n\nedit: put this in model.toml, not pyproject.toml. ad: tune faster with cloud GPUs. signature: still using the 2022 defaults."
    - "cell_type: markdown\nWe are going to inspect the first rows before changing anything. The notebook export dropped the screenshot and pasted the cell toolbar text here.\ncell_type: code\nimport pandas as pd\norders = pd.read_csv('orders.csv')  # downloaded from LMS, not live data\norders.head()\ncell_type: code\norders['total'] = orders['price'] * orders['qty']\n# TODO why is this NaN\norders[['price','qty','total']].tail()"
    - "cell_type: code\nexecution_count: 4\nI don't know why the blank row is still there, but this is the bit that finally made the preview match the spreadsheet.\n```python\nrows = [r for r in rows if r]\n # old file had a handwritten trailer row from the copier\n```\nmarkdown_cell: screenshot filename, two broken image icons"
    - "Please stop deleting the commented port in the sample config. It is there to show the old default, not to set anything:\n```\nservice.name=portal\n # port is 8080\nserver.port=8081\n```\nBasic $9 / Pro $19 / Enterprise contact sales"
    - "A Dockerfile comment is not a command, which is why this line does not install anything:\n```\nRUN uv sync --frozen  # network is blocked in release lane\n```\nCOPY . /app\nCMD [\"./serve\"]\nNewsletter: ship containers with fewer surprises"
    - "$logs = Get-ChildItem .\\logs -Filter *.txt | Sort-Object LastWriteTime\n$latest = Get-Content $logs[-1].FullName -Tail 80\n # --- NIGHTLY FAILURES ---\n$latest | Select-String 'FAILED'\nCompress-Archive .\\logs\\runner.txt .\\logs\\runner.zip -Force\n# TODO\n# Cy 2026-05-04"
    - "func _physics_process(delta):\n    velocity.y += gravity * delta\n    if Input.is_action_just_pressed(\"jump\") and is_on_floor():\n        velocity.y = -jump_force  # coyote timer already applied upstream\n    # TODO camera shake when landing from big drop\n    move_and_slide()\n    if global_position.y > kill_y:\n        respawn_at_checkpoint()"
    - "import csv\nwith open('votes.tsv') as f:\n    rows = csv.DictReader(f, delimiter='\\t')\n    kept = [r for r in rows if r['status'] == 'valid']  # provisional rows excluded here\nprint(len(kept))\n # county PDF margin produces one blank record\n # recount clerk still checks provisional totals in a separate spreadsheet"
    - "ENV_COMMENT_STYLE=hash\nAPP_NAME=kids-library\nAPP_ENV=local\n # Nina 5/4\n# PORT=3000\nBANNER_TEXT=story hour moved to room B\n# TODO\nPRINT_BADGES=false\n# printer in closet jams after warmup"
    - "Install the demo locally:\n```bash\npython -m venv .venv  # use py -m venv on the lab PCs\n. .venv/bin/activate\npip install -r requirements.txt\n# if Pillow fails, update pip first\npython demo.py\n```\n![demo screenshot](demo-2023.png)\nWindows users: run from PowerShell."
    - "class Importer\n  # csv import v2\n  def call(path)\n    rows = CSV.read(path, headers: true)\n    rows.reject { |r| r['voided'] == 'true' }\n    # rows.lazy.reject { |r| r['voided'] == 'true' }\n  end\nend\n # J. Kim"
    - "pipelines:\n  default:\n    - step:\n        name: test-web\n        script:\n          - npm ci\n          - npm test\n          # - npm run e2e\n          - npm run build\n        artifacts:\n          - dist/**\n    # --- RELEASE ---\n  branches:\n    release/*:\n      - step:\n          name: package-only\n          script:\n            - npm ci\n            - npm run build\n            - node scripts/check-size.js\n          # promote by hand"
    - "awk -F, 'NR>1 {print $2,$5}' May.csv > May.names\nsort May.names | uniq -c > name-counts.txt  # duplicate badge scans still useful\n # desk volunteer wrote two guest rows by hand\nwc -l name-counts.txt"
    - "from PyQt6 import QtCore, QtWidgets\n # generated by pyuic6 from main.ui\n# WARNING: All manual changes will be lost\nclass Ui_MainWindow(object):\n    def setupUi(self, MainWindow):\n        MainWindow.setObjectName('MainWindow')"
    - "    humidity = sensor.relative_humidity\n    if humidity is None:\n        missed += 1  # DHT22 returns None when cable wiggles\n        continue\n    samples.append(humidity)\n    # keep the fan off during the classroom demo\n    if len(samples) >= 12:\n        break"
    - "new_users <- subset(users, cohort == 'spring')\nnew_users$retained <- new_users$week4 > 0  # app event renamed from active_days\naggregate(retained ~ channel, new_users, mean)\n # dashboard query still uses the old column name"
    - "15 2 * * * /opt/jobs/ledger.sh  # bank file lands around 01:40\nMAILTO=ops@example.invalid\nPATH=/usr/local/bin:/usr/bin:/bin\n# copied from old cPanel help page\n0 5 * * 1 /opt/jobs/report.sh\nMonitor cron jobs from one dashboard"
    - "import tomllib\n # DEFAULT LIMIT\ncfg = tomllib.loads(Path('worker.toml').read_text())\nlimit = int(cfg['queue']['limit'])\nprint(limit)\n # TW"
    - "csv_comment_char=hash\n1188,ok,144\n # export: kiosk A\n1190,hold,0\n# TODO\n1191,ok,149\n\nValueError on row 2 unless comment='#' is enabled.\nreply count 4\nSponsored: clean CSVs without spreadsheets"
    - "psql $DATABASE_URL -f schema.sql\npsql $DATABASE_URL -c 'vacuum analyze events'\npython verify_counts.py --window yesterday\npython verify_counts.py --window today\n # DST CHECK\n # DBA ticket 812\npsql $DATABASE_URL -c 'select max(imported_at) from events'"
    - "function sync_one(bucket, key)\n    obj = S3.get_object(bucket, key)\n    write(joinpath(cache_dir, key), obj.body)  # paths already normalized upstream\nend\n # winter archive has spaces in keys; leave test fixture"
    - "[pytest]\naddopts = -q\n # --maxfail=1 is too mean for workshop laptops\nmarkers = slow: needs the projector machine\ntestpaths = tests\n# copied from class repo, mostly"
    - "curl -sS $URL > response.json\njq '.items | length' response.json\njq -r '.items[].id' response.json > ids.txt  # saved for follow-up delete batch\n # API started returning archived entries on Tuesday"
    - "notes = raw_notes.str.replace('\\r', '', regex=False)  # vendor export came from Windows tablet\nnotes = notes.str.strip()\nmask = notes.ne('')\nclean_notes = notes[mask]\n # training file keeps blank notes out of weekly report"
    - "BEGIN { FS=\",\"; OFS=\",\"; paid=0; voided=0; phone=0; gift=0 }\n # === DAILY CASH ===\nNR == 1 { print \"status\",\"amount\",\"source\"; next }\n$4 == \"paid\" { paid += $3; kept++; print $4,$3,$7 }\n$4 == \"void\" { voided += $3; next }\n # Marisol 2026-05-06\n# $7 ~ /manual/ { manual++ }\n$7 ~ /phone/ { phone++ }\n$8 ~ /gift/ { gift += $3 }\n$9 == \"Y\" { split_tender++ }  # counter label, not boolean, sure\n # TODO\n$2 == \"\" { empty_name++; next }\n$3 < 0 { negative++ }  # refunds are in another file, allegedly\n/TEST SWIPE/ { test_swipe++; next }\nEND {\n  print \"paid_total\", paid > \"summary.csv\"\n  print \"void_total\", voided >> \"summary.csv\"\n  print \"phone_rows\", phone >> \"summary.csv\"\n  print \"gift_total\", gift >> \"summary.csv\"\n  print \"split_tender\", split_tender >> \"summary.csv\"\n}\n # cc finance-night"
    - "plugins=(auth billing reports)\nfor p in ${plugins[@]}; do\n  ./bin/check-plugin $p\n  ./bin/package-plugin $p\n  ./bin/sign-plugin dist/$p.tgz\n  # release desk\n  # ./bin/upload-plugin dist/$p.tgz\ndone\nsha256sum dist/*.tgz > dist/SHA256SUMS\n # TODO email checksums"
    - "datos <- read.csv('ventas.csv')\n # quitar filas vacias, creo\nlimpio <- datos[datos$total != '', ]\n# ojo: esto no convierte texto a numero\nlimpio$total <- as.numeric(limpio$total)\nsummary(limpio$total)\n # si truena, revisar comas decimales"
    - "path: /srv/drop\nmax_mb: 50\n # --- ACCEPTED TYPES ---\nextensions:\n  - jpg\n  - png\n  - pdf\n# TODO: ask about heic\nretention_days: 21\nquarantine: /srv/drop/quarantine\n# storage team\nvirus_scan: clam\n# probably fine"
    - "for f in *.csv; do\n  # --- CLEAN ---\n  python clean.py $f > clean/$f\n  wc -l clean/$f\n  python check_header.py clean/$f\n  # Noel 2026-05-05\ndone\npython summarize_cleaning.py clean/*.csv > cleaning-summary.txt"
    - "require 'net/http'\nuri = URI(ENV.fetch('CHECK_URL'))\nres = Net::HTTP.get_response(uri) # proxy handles TLS in prod\nabort res.code unless res.is_a?(Net::HTTPSuccess)\nbody = res.body.to_s\nabort 'missing marker' unless body.include?('ready') # old healthcheck accepted 301, this one does not\n # deploy console showed a 200 from the CDN, not the origin\n # runner retries twice before paging because TLS renewal races nginx reload"
    - "Pkg.instantiate()\nusing Parquet, Statistics\n # Q2 MARGIN\nsales = read_parquet('sales.parquet')\nmargin = skipmissing(sales.margin)\nmean(margin)\nquantile(collect(margin), [0.1, 0.5, 0.9])\n # TODO confirm p90 label\nwrite_parquet(sales, 'sales.checked.parquet')"
    - "MAILDIR=/srv/mail/archive\n # MAIL COUNT\npython parse_mbox.py $MAILDIR/inbox.mbox\npython parse_mbox.py $MAILDIR/support.mbox --append\npython summarize_threads.py out/messages.json\npython find_missing_subjects.py out/messages.json\n # privacy ticket PRIV-771\n # do not publish raw subjects"
    - "SET sql_safe_updates = 0;\n# --- LOAD ---\nCREATE TEMP TABLE staged AS SELECT * FROM raw_orders;\n# DELETE FROM staged WHERE total < 0;\nDELETE FROM staged WHERE sku IS NULL;\nUPDATE staged SET channel = lower(channel);\n # M.T. 2026-05-01\nINSERT INTO clean_orders SELECT * FROM staged;\nANALYZE clean_orders;"
    - "export APP_ENV=staging\nexport FEATURE_REVIEWS=false\nexport FEATURE_COUPONS=true\nnpm run migrate\nnpm run smoke\nnpm run smoke:checkout\n # do not run seed task here\n # release ticket RELEASE-912"
    - "df = spark.read.csv(path, header=True)\ndf = df.withColumnRenamed('User Id', 'user_id')\ndf = df.dropDuplicates(['user_id', 'event_time'])\ndf = df.filter(df.status != 'test')\n # --- WRITE ---\ndf.write.mode('overwrite').parquet(target)\n # AH"
    - "cat <<EOF > /tmp/banner.txt\nmaintenance window tonight\nsearch may be unavailable 02:00-02:20\nEOF\npython notify.py /tmp/banner.txt --tenant beta\npython notify.py /tmp/banner.txt --tenant internal\nrm /tmp/banner.txt\n # status ticket STATUS-22\n # status page is separate\njournalctl -u notifier -n 30"
    - "def tick(dt):\n    player.vy += 980 * dt\n    if keys['left']:\n        player.x -= 180 * dt\n    # enemy spawn timer resets on room change\n    if room.just_loaded:\n        spawn_clock = 0\n    return player.x, player.y"
    - "R treats this as a comment so it will not draw a label by itself\n # protocol B\nboxplot(delta ~ group, input)\n\nscore: 7/10. rubric item 'annotate plot' marked incomplete; upload preview wraps the code before the tilde."
    - "base_url = ENV['PORTAL_BASE']\n # portal ping\nclient = Portal.new(base_url)\nclient.get('/health')\n\nNot shell syntax here. Exercise 4 starts below a duplicated signup form and the footer link points back to exercise 2."
    - "set -euo pipefail\ntrap 'echo failed at $LINENO' ERR\npython ingest.py --date $RUN_DATE --source bank  # idempotent after manifest table landed\npython ingest.py --date $RUN_DATE --source card  # card file lands ten minutes later on holidays\npython audit.py --date $RUN_DATE\npython compare_totals.py out/$RUN_DATE/*.csv\n # pager waits for audit, not ingest\n # cron mail copy lost the first environment line\nmail -s ledger-ok finance@example.invalid < out/$RUN_DATE/audit.txt"
    - "set number\nset tabstop=2\n # Maya's old vimrc, do not ask\n# colorscheme desert\nset ignorecase\n# why is search yelling"
    - "grep -v '^$' roster.txt > roster.nonblank\ncut -d, -f1 roster.nonblank | sort > names.txt  # only first column feeds badges\ncomm -23 names.txt checked_in.txt\n # badge station tablet adds a trailing comma"
    - "echo ok  # yes this is the whole smoke test"
    - "set -e\nrsync -av --delete public/ web:/var/www/site/\n # deploy notes: https://intranet.example.invalid/runbooks/static-site\nssh web 'nginx -t'\nssh web 'systemctl reload nginx'\nprintf '%s\\n' $BUILD_SHA > deploy.sha\n# TODO purge CDN\n# Lila was here\ntail -n 20 /var/log/nginx/error.log"
    - "config :logger, level: :info\nconfig :app, cache_seconds: 120\n # staging override lives in runtime.exs\nconfig :app, mailer: App.Mailer\n # MAILER\nconfig :app, allowed_hosts: ['portal.example.invalid', 'preview.example.invalid']\nconfig :app, audit_sink: :database\n # Cristina"
    - "records = JSON.parse(File.read('events.json'))\nrecords.select! { |r| r['kind'] != 'debug' }\nrecords.each { |r| r.delete('device_id') }\n # PRIVACY PASS\nFile.write('events.clean.json', JSON.dump(records))\nputs records.length\n # TODO ids audit"
    - "hugo --minify\nfind public -name '*.html' -print0 | xargs -0 ./check-links  # ignores mailto links\n # archive pages have duplicate ids but render fine\naws s3 sync public s3://static-site/"
    - "with open('labels.txt') as labels:\n    for raw in labels:\n        name = raw.strip()\n        if not name:\n            continue\n        if name.startswith('VOID'):\n            continue\n        print(name.upper())\n # VOID rows\n # envelope batch\nfooter = 'printed from lobby laptop'\nprint(footer)"
    - "tmpdir=$(mktemp -d)\n # XLSX CONVERT\ncp *.xlsx \"$tmpdir\"\npython convert.py \"$tmpdir\"\nrm -rf \"$tmpdir\"\n # email both formats"
    - "[playlist]\ntitle=rainy bus ride\n # songs Mina keeps skipping\ntrack1=Window Seats\ntrack2=Late Ferry\n# track3=Accordion Practice\ntrack4=North Pier\n# 2026 mix"
    - "FROM node:22-alpine\nRUN corepack enable\nCOPY package.json pnpm-lock.yaml ./\nRUN pnpm install --frozen-lockfile  # preview deploy uses offline mirror\nCOPY . .\nRUN pnpm build\n # final image strips dev dependencies in separate stage"
    - "table = pa.Table.from_pandas(df, preserve_index=False)\npq.write_table(table, out_file)  # parquet preserves null category labels\nprint(out_file.stat().st_size)\n # downstream Spark job chokes on zero-byte uploads"
    - "func tableView(_ tableView, numberOfRowsInSection section):\n    return len(messages)\n# copied from the iOS walkthrough, indentation is fake python now\ncell = tableView.dequeueReusableCell(withIdentifier: \"msg\")\ncell.textLabel.text = messages[indexPath.row]\n # TODO this crashes when the draft row is hidden"
    - "From: grants desk\nSubject: review packet checksum\n`ssh grants 'sha256sum /incoming/review.pdf'  # compare with local digest before notifying PI`\nSend both checksums in the same reply.\nThis message may contain confidential information. This message may contain confidential information."
    - "A line that starts with the marker can label your section while you are experimenting.\npage 7\n # table 4\nwrite.csv(summary, 'dept_cost.csv', row.names=FALSE)\npage 7\nnext: read.csv and factors"
    - "root = Path('/srv/import')\nfor child in root.iterdir():\n    if child.suffix == '.tmp':\n        child.unlink()\n    elif child.name.endswith('.old'):\n        archive(child)\n # do not delete .partial\n # Sam\nlog.info('remaining files %s', len(list(root.iterdir())))"
    - "setopt pipefail\nprint -r -- $RUN_ID\n # laptop cron\npython reconcile.py $RUN_ID\npython reconcile.py $RUN_ID --bank secondary\npython mail_result.py $RUN_ID\n # finance ticket FIN-303\nprint -r -- done:$RUN_ID >> ~/Library/Logs/reconcile.log"
    - "192.168.1.8 printer\n192.168.1.12 kitchen-tablet\n # old router leased these forever\n# 192.168.1.44 guest-laptop\n192.168.1.51 camera-door\n# Bob wrote this on the modem box"
    - "import duckdb\ncon = duckdb.connect('warehouse.duckdb')\n # parquet export\ncon.execute('copy visits to visits.parquet')\ncon.close()\n # intake ticket INTAKE-17"
    - "VENV=.venv\n$(VENV)/bin/python -m pytest tests\n$(VENV)/bin/python -m coverage xml\n$(VENV)/bin/python -m coverage report --fail-under=82\n # coverage lane\nrm -rf .pytest_cache\n # delete me after Jenkins retirement"
    - "queue: mail\nretry: 3\n # vendor SMTP rejects bursts above 20/min\ntimeout_seconds: 15\nbackoff_seconds: 90\ncache: true\n# banner text comes from CMS, not this file\ncache_ttl: 300\nworkers: 2\n# night shift asked for slower retries after duplicate receipts"
    - "A: the tabs are real tabs, not spaces\nB: add a comment then, like ` # tabs, please`\nA: I did, and now everyone can stop re-saving the file in Excel\nC: wait what is awk anyway\nB: tiny language, large feelings"
    - "df <- readRDS('weekly.rds')\n # roster pass\ndf <- df[!duplicated(df$id), ]\ndf$site <- trimws(df$site)\ndf <- subset(df, status != 'test')\nsaveRDS(df, 'weekly_dedup.rds')\n # old graph expects this filename"
    - "for step in 1:nsteps\n    laplacian!(tmp, u)\n    @. u = u + dt * alpha * tmp\n    # Neumann boundary, copied from lab handout\n    u[1, :] .= u[2, :]\n    u[end, :] .= u[end-1, :]\nend\n # figure code begins after this cell"
    - "import json\n # privacy pass\ntry:\n    payload = json.loads(raw)\nexcept json.JSONDecodeError:\n    payload = {}\nlogger.info('payload keys %s', sorted(payload))\nif payload.get('source') == 'kiosk':\n    payload['source'] = 'front-desk'\nfor key in ('email', 'phone'):\n    payload.pop(key, None)\n # TODO add address someday, kidding, do not\n # privacy ticket PRIV-2041"
    - "make deploy-staging  # staging only\ncurl -fsS \"$URL/health\""
    - "mutate(total = rowSums(across(starts_with('q')))) |> \n  filter(total > 0) |>  # blank surveys removed after scoring\n  group_by(site) |> summarise(n = n())\n # line wrap from quarto render split the pipe here"
    - "python manage.py collectstatic --noinput\npython manage.py migrate --check  # fail deploy if migrations pending\npython manage.py check --deploy\n # static bucket sync happens in platform hook"
    - "t = 0:0.01:2*pi;\ny = sin(3*t) + 0.2*randn(size(t));\nys = movmean(y, 9);  # smoothing window from problem 6\nplot(t, ys)\nhold on  # raw trace hidden for projector contrast\n% legend lost when LMS flattened the notebook"
    - "requests==2.32.3\nPillow==10.4.0  # school photo cropper\n# opencv-python==4.10.0.84\npython-dateutil==2.9.0\n# pin after science fair laptop stopped compiling wheels"
    - "sqlite3 local.db 'select count(*) from events;'\n # laptop triage\nsqlite3 local.db '.backup backup.db'\npython prune.py local.db\n # DB-LOCAL"
    - ". venv/bin/activate\npython -m pip install -e .\n # DEMO\npython examples/load_demo.py\npython -m app.cli doctor\npython -m app.cli export-demo --limit 50\nrm -f demo.sqlite\n# setup.py line is gone"
    - "SCREEN_WIDTH=1280\nSCREEN_HEIGHT=720\n # projector in room 214 cuts off bottom row\nFULLSCREEN=false\n# Auntie mode: big captions\nCAPTION_SIZE=28\n# no one remembers where this file lives"
    - "mkdir -p exports/monthly\npython export_orders.py --month 2026-04 > exports/monthly/orders.csv\npython export_refunds.py --month 2026-04 > exports/monthly/refunds.csv\npython export_adjustments.py --month 2026-04 > exports/monthly/adjustments.csv\nwc -l exports/monthly/*.csv\n # ZIP STEP\n # accounting initials: rp"
    - "apiVersion: v1\nkind: Pod\nmetadata:\n  name: catalog-importer\nspec:\n  containers:\n    - name: worker\n      image: importer:2026.05\n      # catalog importer\n      resources:\n        limits: {memory: 512Mi, cpu: 500m}\n      # bump after launch?\n# k8s-1427"
    - "lines = raw.splitlines()\nheader = lines[0].split(',')\nbody = lines[1:]\nassert 'email' in header\nif header.count('email') != 1:\n    raise ValueError('bad header')\nbody = [row for row in body if row.strip()]\n # keep row order\nwrite_debug('cleaned_rows.txt', body[:20])\n # export ticket EXPORT-404"
    - "set -e\n# Counts are totally always right\nmysql --local-infile=1 reporting < load.sql\nmysql reporting -e 'select count(*) from staging_orders'\nmysql reporting -e 'select count(*) from staging_orders where total < 0'\n # count 3120? 3142? sure.\nmysql reporting < promote.sql\nmysql reporting -e 'analyze table clean_orders'"
    - "project = ARGV.fetch(0)\nsystem('gh', 'workflow', 'run', 'deploy.yml', '-f', \"project=#{project}\") # wrapper for release desk\n # avoid interactive gh prompt on shared machine\n # shared laptop still has cached credentials from last month's hotfix drill\nputs \"queued #{project}\""
    - "openapi-generator-cli generate -i spec.yaml -g typescript-fetch -o sdk\nnpm --prefix sdk test\nnpm --prefix sdk run lint\n # SDK README\nsed -n '1,80p' sdk/README.md\nrm -rf sdk/.openapi-generator\nnode scripts/check-sdk-size.js sdk\n # WARN accepted"
    - "result = pl.scan_csv('meters.csv')\nresult = result.unique(subset=['site', 'read_at'], keep='last')\n # METER EXPORT\nresult.sink_parquet('meter.parquet')\nStreaming sinks require a lazy frame; eager frames collect first."
    - "curl -O https://example.invalid/export.csv\ncurl -O https://example.invalid/sums.txt\npython verify_hash.py export.csv sums.txt\n # checksum follow-up\npython ingest_export.py export.csv\npython audit_export.py export.csv --sample 25\n # ask vendor for signed sums"
    - "if [[ -f .env.local ]]; then\n  set -a\n  source .env.local\n  set +a\nfi\n # local demo\nnpm run dev\nnpm run seed:preview\n # demo ticket DEMO-44\nprintf ready > /tmp/demo-ready"
    - "df['name_key'] = df['name'].str.lower().str.replace(' ', '')\nroster['name_key'] = roster['name'].str.lower().str.replace(' ', '')\n # nickname pass\nmatched = df.merge(roster, on='name_key', how='left')\nmissing = matched[matched.badge_id.isna()]\nmatched.to_csv('matched.csv', index=False)\nmissing.to_csv('missing_badges.csv', index=False)\n # Roberta = Bobbie"
    - "options(warn=1)\n # daily-summary entrypoint\nmain <- function() {\n  args <- commandArgs(trailingOnly=TRUE)\n  stopifnot(length(args) == 1)\n  date <- as.Date(args[[1]])\n  print(date)\n  source('daily-summary.R')\n}\nmain()\n # Ren 2026-05-02"
    - "backup_dir=/mnt/backup/$(date +%F)\nmkdir -p $backup_dir\ncp warehouse.duckdb $backup_dir\ncp manifest.json $backup_dir\nls -lh $backup_dir\npython verify_backup.py $backup_dir/warehouse.duckdb\n # backup drill DRILL-19\n # Mira"
    - "def checkout_total(cart):\n    # TODO 2018 remove after coupon rewrite\n    subtotal = sum(item.price for item in cart.items)\n    # damn it, gift cards are negative items in this system\n    credit = sum(item.price for item in cart.items if item.kind == 'gift_card')\n    # TODO 2019 stop doing accounting in the view\n    tax = round(subtotal * 0.0825, 2)\n    # FIXME coupons after tax? ask Priya\n    total = subtotal + tax + credit\n    # legacy receipt wants this exact variable name\n    return total\n\n# copied into wiki page 4, then an ad for payroll software lands here\n# TODO 2020 remove after new checkout\n# TODO 2021 remove after new checkout, really\n# TODO 2024 still here, cool\nsample_cart = type('Cart', (), {'items': []})()\nprint(checkout_total(sample_cart))"
    - "chart <- ggplot(df, aes(day, count)) + geom_line() + facet_wrap(~region)\nchart <- chart + scale_y_continuous(labels=scales::comma)\n # comms ticket COMM-128\nggsave('daily-count.png', chart, width=8, height=4)\nwrite.csv(df[df$count == 0, ], 'zero-days.csv', row.names=FALSE)\n # alt text later"
    - "def draw_minimap(screen, rooms, player_room):\n    for room in rooms:\n        if not room.seen:\n            continue\n        color = (80, 80, 80)  # undiscovered exits stay invisible\n        if room is player_room:\n            color = (255, 230, 90)\n        pygame.draw.rect(screen, color, room.rect)\n        # cut off here in the wiki export"
    - "dist/\n.cache/\n # keep scanned receipts out of repo\nreceipts/*.jpg\n!receipts/sample.jpg\n# choir folder exports Thumbs.db every time\nThumbs.db\n# TODO split school and home ignores"
    - "from pathlib import Path\ncache = Path('cache')\nfor file in cache.glob('*.tmp'):\n    file.unlink()\nfor file in cache.glob('*.bak'):\n    archive(file)\nprint('remaining', len(list(cache.iterdir())))\n # screenshot folder ignored\n # lockfiles stay until preview exits"
    - "set -e\n# report build\nRscript make_tables.R\nRscript make_figures.R\npandoc report.md -o report.docx\npython collect_comments.py report.docx\n # TODO\npython summarize_comments.py comments.json\n # Reviewer 2, again"
    - "2026-05-01,requests,9981\n2026-05-01,errors,12\n # dashboard percent column\n2026-05-01,latency_ms,184\n2026-05-02,requests,10022\n2026-05-02,errors,9\n2026-05-02,latency_ms,179\n# local time\n# analyst note"
- token: " #"
  meaning_label: "hex_color_prefix"
  text_samples:
    - "The cave map was unreadable until I changed copper node color to #b87333 and left tin color at #6f8faf. Spawn rings stay yellow because new players actually notice them; swamp fog can be darker later."
    - "M104 S215 ; recovered printer project\n; preview colors survived but object names did not\nfilament_colour = #f77f00\nsupport_material_colour = #adb5bd\nwipe_tower_preview = #003049\ntravel_moves = hidden\nLAYER:12\n; thumbnail begins 200x200\n; png palette line says shell color #d62828 infill color #fcbf49 brim color #eae2b7\nG1 X114.2 Y88.4 E0.032\nG1 X115.0 Y88.9 E0.027\n; slicer crashed here, next block is from a different vase model\nmodel_color = #6a994e\nseam_paint = #bc4749\nad text: premium nozzles free shipping free shipping\nM117 change spool soon\n; phone photo OCR turned PETG into PFTG and inserted a temperature table between shell and support. The project notes continue with nozzle wear, table wobble, glue stick brand, and a copied receipt footer before the final color swatch #386641."
    - "{\"zone\":\"kitchen\",\"segments\":[{\"start\":0,\"end\":23,\"hex\":\"#ff7a18\"},{\"start\":24,\"end\":47,\"hex\":\"#00d1ff\"}],\"transition_ms\":900}\npreview strip: start color #ff7a18, chase color #00d1ff\nBLE 02 4f 7a 18 00 d1 ff"
    - "Use the blue from the truck decal, not the website header. The printer proof calls the blue color #0057b8; the gold stripe color is #f2a900 and should only appear on the coupon edge. Body copy stays near black."
    - "colors:\n  copper: #b87333\n  steel: #71797e\n  enamel_white: #f8f5ed\n  gasket_black: #101820\n  warning_orange: #ff8200"
    - "filament_name = PETG coral\nfilament_colour = #ff6f61\nsupport_material_colour = #8d99ae\nfirst_layer_bed_temperature = 80\nfirst_layer_speed = 20\n; brim preview uses the same coral swatch in the slicer"
    - "/* color tokens */\n:root {\n  --accent-color: #ff006e;\n  --accent-soft-color: #ffd6e8;\n  --copy-color: #202124;\n  --paper-color: #ffffff;\n}"
    - "<Rule><Name>floodway</Name><LineSymbolizer><Stroke><CssParameter name='stroke'> #0077b6</CssParameter><CssParameter name='stroke-width'>2</CssParameter></Stroke></LineSymbolizer></Rule><TextSymbolizer>parcel labels merged below road layer</TextSymbolizer>"
    - "@media(max-width:600px){.hero{background:#2b2d42;color:#edf2f4}.skip{outline:2px solid #ff9f1c}.adslot{display:none}}.print{color:#22223b}sourceMappingURL=main.css.map"
    - "For the county trail handout, I kept water color at #1d70a2 and changed temporary closure color to #fb8500 so it survives grayscale printing. Trail labels remain dark because the parks layer already carries the green."
    - "INVOICE TERMS  NET 30  page 2 of 2\nship to line wrapped over logo area\ncrop mark color noted as #cccccc, watermark color screened at #eeeeee, signature rule color #666666\nS55-0140    NET 30    remit to lockbox 18"
    - "page\tbox\tfill\tink\n4\tscholarship_banner\t #004c97\t#ffffff\n4\tdonor_name_panel\t#f7f3e8\t#2d2a26\n6\trule\t#b9975b\t#2d2a26\nproof_rev\t7\t\t"
    - "<!--[if mso]><v:roundrect fillcolor='#31572c' strokecolor='#132a13'><![endif]--><a style='background:#31572c;color:#f7f7f2;border:1px solid #132a13'>Confirm</a><!-- fallback color #4f772d for Outlook 2016 -->"
    - "cluster_color_map = {0:'#440154', 1:'#21918c', 2:'#fde725'}\nplt.scatter(x, y, c=cluster_id.map(cluster_color_map))\nplt.title('assay drift, plate B')\nlegend swatch 0 color #440154 is printed too dark in the lab handout"
    - "Color theory note: #6d597a reads quieter than pure violet, especially next to #e56b6f and #eaac8b. I used it for the shadow side of the palette in the quilt mockup."
    - "diff --git a/dashboard.css b/dashboard.css\n- --panel-warning-color: #fef3c7;\n+ --panel-warning-color: #fde68a;\n- --panel-subtle-color: #6b7280;\n+ --panel-subtle-color: #52525b;\n  --panel-bg-color: #ffffff;\n  --panel-border-color: #d1d5db;\n  --panel-title-color: #111827;\ncommit 8c19 theme polish"
    - ".shipping-alert{background: #9d0208;border-color:#6a040f;color:#fff3b0}.shipping-alert a{color:#ffba08}.footer{color:#5c677d}"
    - "<svg width='24' height='24' viewBox='0 0 24 24'><path style='fill: #334155' d='M3 12h18v2H3z'/><circle cx='12' cy='8' r='4' fill='#f59e0b'/><path stroke='#cbd5e1' d='M4 20h16'/><text fill='#dc2626' x='6' y='18'>!</text></svg>"
    - ".mini-cart .payment-warning { background-color: #fef3c7; color: #92400e; }\n.mini-cart .price { color: #111827; }\n.mini-cart :focus { outline-color: #93c5fd; }"
    - "The signup button looked heavier in German because the label wrapped, not because of the purple. Keep the background #3d348b, set disabled copy color #6b7280, and leave the warning box alone until copy review."
    - "PCB_REV_C notes\nsoldermask: matte black, silkscreen ink #ffffff\nfab asked whether polarity mark color should stay #ffbf00 on assembly drawing; exported drill table starts immediately after the color note."
    - "Subject line test B keeps the coral header. The HTML has `<td bgcolor=\"#e56b6f\">` and a teal link color #006d77; the plain-text version just says renewal weekend and then the unsubscribe footer."
    - "<svg viewBox='0 0 400 80'><rect width='400' height='80' style='fill: #0b3d91'/><text x='18' y='52' fill='#f4d35e'>OPEN HOUSE</text><path d='M0 79h400' stroke='#ee964b'/></svg>"
    - "Return-Path: promo\nContent-Type: text/html\n<td height='48' style='background:#0f766e;color:#ffffff;border-top:1px solid #99f6e4'>Renew now</td>\n-- clipped --\nplain text fallback: expires soon / Android preview sampled color #0d9488"
    - "<p:sp><p:nvSpPr/><p:spPr><a:solidFill><a:srgbClr val=\"fef3c7\"/></a:solidFill><a:ln><a:solidFill><a:srgbClr val=\"f59e0b\"/></a:solidFill></a:ln></p:spPr><p:txBody><a:t>trial spike</a:t></p:txBody></p:sp> speaker note calls the border color #f59e0b"
    - "fern slip color #4f772d / iron wash color #7f5539 / glaze test tile says sky color #90e0ef\nKiln note: cone 6 looked washed out; the barcode label bled into the next recipe row."
    - "<tr><td style='color: #2b2d42'>Thanks for the order, Marta.</td></tr><tr><td style='padding:16px;background:#edf2f4'>Pickup window: Friday 4-7</td></tr><tr><td><a style='background:#ef233c;color:#ffffff;border-color:#d90429'>View receipt</a></td></tr>"
    - "body background #ffffff, link color #0645ad, visited link #0b0080, table border color #a2a9b1, notice background #fff3cd, notice text #664d03. Weather: 72 F, light rain, updated 4:10."
    - "14:31:02 export_pdf page=7 warning=transparent_fill resolved_to_color #00000000; watermark skipped; next line is a font subset error and 200 lines of xref offsets."
    - "git show --word-diff theme-dark\n--bg: [-#020617-]{+#0b1120+}; --panel: #111827; --text: #e5e7eb;\n--muted: [-#64748b-]{+#9ca3af+}; --link: #93c5fd;\nterminal preview appended cursor color #f8fafc selection color #334155 after the diff hunk"
    - ".alert-critical { background-color: #f95738; color: #ffffff; }\n.alert-critical a { color: #ffe66d; }\n.alert-muted { background-color: #f4f1de; color: #3d405b; }"
    - "node,property,value,ms\nhero,background, #fdfcdc,12\noverlay,box-shadow,#00000066,86\ncaption,color,#0081a7,2\nbutton,background,#f07167,5\nsummary,layout-shift,,41"
    - "ja_JP continue button still fits at 320px with background #457b9d. de_DE needs the shorter verb; do not darken #31572c just to hide the wrap. Focus ring sample is #93c5fd."
    - "theme_mods_twentyx={\"link_color\":\"#005eb8\",\"hover_color\":\"#004b93\",\"footer_text_color\":\"#777777\",\"body_color\":\"#444444\",\"paper_color\":\"#ffffff\"}\nrendered anchor color #005eb8 against paper #ffffff"
    - "I use color #219ebc for the lake layer because the county shapefile already uses green for parks; the rest of the post is about projection errors, not colors, and half the code block is missing."
    - "<table style='background: #fef3c7'><tr><td style='color: #78350f'>Weather delay</td></tr></table>\ntext fallback: alert yellow #fef3c7 / brown copy #78350f / border color omitted #f59e0b"
    - "<li class='error'>Upload failed <span style='color: #d00000'>retry</span></li><li>terms accepted</li><li>cookie notice inserted here</li><script>initQueue()</script>"
    - "storybook-args.json {\"card\":{\"shadowColor\":\"#00000014\",\"background\":\"#fff8f0\",\"border\":\"#c9ada7\"},\"figma\":{\"shadowColor\":\"#0000001a\"},\"viewport\":\"desktop\"}\nbox-shadow color #00000014 in canvas snapshot"
    - "cursor color #ffffff\nselection color #264f78\nerror color #f44747\nwarning color #ffcc00\nansiBlue color #569cd6\nansiGreen color #6a9955\nfindMatch color #515c6a\nterminal bell disabled, profile name missing after import"
    - "posted 2009-11-08: my sidebar vanishes unless I set table border color #999999. sorry for the all caps, this template came from FrontPage and I am afraid of it."
    - "14:42 QA: video preroll loads, sponsored label color #b45309 sits under the mute button, close icon shifts left on iPad, tap outside overlay does not dismiss\nattachment names: ipad_landscape_2.png, preroll_touchmap.csv"
    - "[colors]\nforeground= #f2e9e4\nbackground=#22223b\ncursor=#c9ada7\nbell=none\nfont=Iosevka 11\nprofile_note=copied from the laptop before replacing the SSD"
    - "When the user tabs out of ZIP code, the help text should stay gray #475569; the current build flashes it blue like a link. Border color #cbd5e1 is fine. The bug is the focus handler, not the palette."
    - "Seed packet mockup, back panel: the organic badge fill uses #a7c957 with dark olive text #283618. Leave padding at 6 mm because the cutter mark already crowds the basil photo."
    - "PR note after screenshots: action hover color #457b9d replaced the old blue because it disappeared on projectors. Danger hover remains #b91c1c; QA only asked about the focus ring."
    - "CAUTION box begins mid-sentence after page break. Left rule color #f59e0b appears in the tagged PDF as an annotation; warning text continues with torque values, part numbers, and a smudged safety icon."
    - "Specimen A: label edge intact; Specimen B: cap cracked. The analyzer report highlights qc_warning in color #bc4749, then resumes with sodium and chloride ranges."
    - "body{margin:0}.x{background:#3d348b}.x a{color:#f7b801}.x b{border-bottom:3px solid #f18701}.cookie{position:fixed}"
    - "pptx unzip: ppt/slides/slide18.xml\n<a:srgbClr val='f59e0b'/><a:solidFill><a:srgbClr val='374151'/></a:solidFill>\nnotesSlide18.xml says warning bars previously used color #f97316; speaker wants red #dc2626 only for negative metrics."
    - "home jersey navy #00205b, away stripe #c8102e, number fill #ffffff\ncoach signature: R. Vale\nsocks: navy with red cuff"
    - "Holiday red #b91c1c\nSchool blue #2a6fdb\nBoard meeting gray #4b5563\nEarly release amber #d97706\nSnow day white #ffffff"
    - "release.json badges:{\"beta\":{\"bg\":\"#ede9fe\",\"fg\":\"#5b21b6\"},\"new\":{\"bg\":\"#dcfce7\",\"fg\":\"#166534\"}}\nBeta badge background #ede9fe appears in the generated changelog beside the sqlite wrapper note."
    - "DevTools copy from the article body shows color: #111827 and background: #ffffff. The reader complaint was about font size; link blue #0969da is just what happened to be selected in the Styles pane."
    - "<section style='background: #111827;color:#ffffff'><h1>Newsletter</h1><a style='background:#2563eb;color:#ffffff'>Join</a><small style='color:#93c5fd'>Privacy terms</small></section>"
    - "code bg #f6f8fa, link color #0969da, visited color #8250df."
    - "TRAY 14  HOLD FOR ICE PACKS\nbarcode 77104-8 smeared; QA circle color #80b918 appears near the corner, then the scanner repeats the weight column without units."
    - "My terminal theme is mostly Solarized leftovers, but the cursor color is #f8f7ff because the old white blink was the only thing visible on the projector. Error red #ef476f is probably too cheerful."
    - "thread,brand,hex\noutline,Madeira, #1b1b1e\npetal,Robison,#ffafcc\nleaf,Robison,#386641\nbobbin note says machine skipped color 3 after thread break"
    - "btn_primary_color= #457b9d\nbtn_text_color=#f1faee\ntrial_badge_color=#e63946\nstars_color=#ffb703\nprivacy_link_color=#6d6875\nbbox_price=219,441,388,492\nlang=ja_JP"
    - "update campaign set hero_hex='#118ab2' where slug='river-cleanup';\nselect 'approved color #118ab2' as proof_note;\nupdate campaign set note='copy approved, photo still blurry' where slug='river-cleanup';"
    - "{\"frame\":118,\"colorSamples\":{\"badge\":\"#0284c7\",\"soldOut\":\"#dc2626\",\"backIcon\":\"#111827\",\"filterBorder\":\"#cbd5e1\"},\"controls\":[\"beds\",\"accessible\",\"price\"],\"blurred\":true}\nvisual diff badge color #0284c7"
    - "00:00.912 tap hamburger\n00:01.004 sheet visible, handle color #d4d4d8 sampled against white\n00:01.202 TalkBack focus jumps to close button\n00:02.118 user swipes past active item; gesture diagram covers the lower nav"
    - "Subject: renewal notice\n<!--[if mso]><v:roundrect fillcolor='#006d77' strokecolor='#264653'><![endif]-->\n<a style='background: #006d77;color:#edf6f9;border-color:#264653'>Renew</a>\n<td style='color:#8d99ae'>unsubscribe</td>"
    - "risk_score=0.82 badge_color= #ffd166 reviewer=null comments=[]\ncompany=\"Baker, Moss & Hale\"\ncompliance_text=\"manual review required\""
    - "file,page,x,y,text,fill\nbrochure.pdf,2,118,441,Free admission, #073b4c\nbrochure.pdf,2,119,470,Saturday only,#ef476f\nOCR_note,line_break_in_price,"
    - "SPEAKER_02: the orange line color #f28e2b sampled but reads brown from row six\nSPEAKER_01: leave the grid #e0e0e0, we need to fix the axis title first\n[inaudible] slide number covers legend"
    - "storybook-static/iframe.html?args=focus\nControls panel collapsed, axe tab open, keyboard outline color visible as #93c5fd around Save. The DOM snapshot also contains danger copy #dc2626 from a hidden example, then dumps addon telemetry."
    - "freezer aisle labels fell off pallets 31-34 after washdown. Zone A replacement sticker color is #2a6fdb; temporary hold tags are pale yellow so receiving does not confuse them with route tape."
    - "flour paragraph, jump link, sponsor disclosure, then `<aside style='background: #fff7ed;border-color:#fed7aa'>` before the oven-temperature note. Comments about dough hydration start before the closing aside tag."
    - "slide12_export.xml <a:solidFill><a:srgbClr val=\"fef3c7\"/></a:solidFill><a:fontRef idx=\"minor\"><a:schemeClr val=\"tx1\"><a:lumMod val=\"95000\"/></a:schemeClr></a:fontRef><p:attr name=\"chalkTextColor\" val=\"#f8fafc\"/> speaker note: chalk text color #f8fafc"
    - ".skeleton-card{background: #ffffff}.skeleton-card::before{background:#e5e7eb}.related-link{color:#0969da}.product-title{color:#24292f}"
    - "header color #1f2937 / agenda item color #374151 / amendment highlight #fef3c7 / vote yes color #166534\nvote no color #991b1b split onto next OCR line\nabstain color #a16207 postponed color #6b7280 merged into public-comment section"
    - "cursor vanishes on the password prompt, selection text copies with stray spaces, and the split pane loses focus after resize. The theme caption says cursor color #f8fafc, which should be visible; likely the prompt is drawing over it."
    - "participant found the focus ring faster when it rendered color #f59e0b, then lost track of the table after the modal opened. Recording frame 04 shows body text #000000; facilitator transcript spends four turns on keyboard order."
    - "The recipe page loads a salmon banner color #ff6b6b above the flour paragraph.\nADVERTISEMENT Dishwasher tablets 20% off\n2 cups flour\n1 tsp salt"
    - ".tab { background: #e8eddf; color: #333d29; }\n.tab.active { background: #588157; color: #fefae0; }\n.tab.disabled { background: #dad7cd; color: #6b705c; }\nkeyboard focus outline color #dda15e appears in the generated docs, not in source CSS.\nTabs / Forms / Dialogs / Tabs"
    - "Union notice proof, page 1: body ink reads #111111 in the PDF inspector, the rule under the date has color #b7b7a4, and the green bug beside the logo is too close to the trim."
    - "The methods appendix says agreement was colored green #16a34a and disagreement red #dc2626 in all figures. No-answer cells were gray, but the print copy makes neutral and missing responses hard to separate."
    - "--boundary_42\n<td bgcolor=\"#fef3c7\" style=\"color:#713f12;border:1px solid #f59e0b\">15% renewal credit</td>\n<td style=\"color:#64748b\">unsubscribe</td>\nlegal disclaimer repeats under the tracking pixel"
    - "Support macro: paid receipts get a pale green band #dcfce7 and voided receipts get pale red color #fee2e2. If the customer sends a screenshot with a red total but no VOID stamp, escalate to billing."
    - "Removed Records: Comments from /xl/comments1.xml\nRecovered hidden cell F42 fill color #7e22ce and formula note about visited-link background #f3f4f6. Workbook opens, but freeze panes are gone."
    - "popup Bridge 14 opens under bus layer\nlabel detour_stroke_color= #f59e0b closure_text_color=#dc2626\nhit_test roadseg_184, roadseg_185, bus_stop_07\nlayer order stale after tile reload"
    - "The kiosk idle screen is nearly black #0b090a, so the receipt preview looked broken in the night photo. Active checkout blue is #2563eb; error text is the red line under the card field."
    - "invoice overdue label color #991b1b sampled, draft watermark color #525252 sampled, row highlight color #fef9c3. The rest of the annotation is a billing address correction and a request to move the signature line."
    - "headline_lines=3 accent_color= #38bdf8 shadow_color=#00000040 crop=1200x630 alt_text=\"\" spinner=frozen publish_button=visible autosave_state=pending"
    - "Lost child form printed over the evacuation map; the only visible map key is shelter area color #90be6d. Parent phone number is hidden by the copier shadow."
    - "table header rendered twice, first with background #f8fafc and then as a ghost row over the totals. Border color #e2e8f0 appears in the PDF text stream before the subtotal labels."
    - "why does the dark theme use gray #18181b instead of black #000000? surface color #18181b reduces blooming, copy color #fafafa remains readable\ndocs excerpt: surface color #18181b reduces blooming\ndocs excerpt: surface color #18181b reduces blooming"
    - "button still says Submit; view-source shows inline style color #ffbe0b but the live page paints blue after hydration."
    - "0.0-0.2 chlorophyll color #edf8fb\n0.2-0.5 chlorophyll color #b2e2e2\n0.5-1.0 chlorophyll color #66c2a4\n1.0+ chlorophyll color #238b45\ncloud_mask legend: 0 clear / 1 probable / 2 cloud\ndissolved_oxygen = mg_l * correction"
    - "PDF text layer around the town calendar: masthead rectangle fill #2a6fdb from y=0 to 88, button fill #2563eb near the lower-right, footer ink #64748b. The meeting dates are plain text."
    - "<td width='600'>\n<p>June volunteer shifts are open.</p>\n<p style='border-left:6px solid #43aa8b'>Garden crew meets behind the shed.</p>\n<p>Forwarded footer starts here, unsubscribe link missing.</p>\n</td>"
    - "dark keyboard shortcut chips render background #374151; copied text says Ctrl Shift P without spaces. The accessibility comment below asks for focus outline #93c5fd in the interactive demo."
    - "SPEAKER_01: meet at first aid tent, red cross icon color #dc2626 sampled on the map\nSPEAKER_02: crafts aisle is purple #8b5cf6, not blue\nSPEAKER_01: map is folded over gate B, send a volunteer"
    - "val InfoBg = Color(0xFFDBEAFE) // #dbeafe\nval ErrorBg = Color(0xFFFEE2E2) // #fee2e2\nval Submit = Color(0xFF2563EB) // #2563eb\nval Helper = Color(0xFF64748B) // #64748b\npreview screenshot lost the status bar"
    - "prompt_color= #f72585\nhistory_limit=2000\nshow_clock=true\nselection_color=#4cc9f0\nmotd=backup complete maybe\nprofile_note=used on the pink kiosk shell"
    - "Rejected stamp color #dc2626 appears under the example form, but the lesson text is about escalation timing. Watermark color #e5e7eb crosses the lower-right corner.\nPAGE 14\nPAGE 14"
    - "Every time I paste the chart into the wiki, the legend color turns #7209b7 and the row labels wrap under the ad column. I do not know CSS; I am just trying to post the bus survey."
    - "- modal close focus outline color samples #93c5fd in visual diff\n- disabled form package removed from bundle\n- payment copy updated for Canada\nScreenshot label mentions danger hover color #991b1b because the capture script reused a frame name."
    - "Agent: I can resend the return label.\nVisitor: the upload button vanished.\nSystem notice background #fef3c7: refund macro sent\nbubble border color #bae6fd"
- token: " #"
  meaning_label: "numbered_item_rank"
  text_samples:
    - "<img alt='DryStep Kids ranked #1 rain boot'><nav>clearance / toddlers / boots</nav><article class='sponsored'>MuddyDay</article><p class='review'>old ankle boot sat at rank #7 last winter and kept the lining dry</p>"
    - "Subject: weekly playlist export complete\nKQMR weekly chart has Lamp Oil at #1. North Pier appears in the widget URL as rank=%233; LICENSE NOTICE\nunsubscribe | station photos | pledge drive"
    - "meet standings after heat 4: Morgan took #1 after 57.81; Silva moved to #2; Imani stayed #6 after the wind delay.\nLANE SPLITS\n1 13.9 14.4 14.8 14.7\nRUNNING SHOES closeout sizes 6-9"
    - "finish camera packet 18B / varsity four / Ridge Crew finished #1 / elapsed 06:42.8 / buoy review confirmed / <td class=ad>SPRING REGATTA SALE</td> / Harbor line follows in hidden column"
    - "agenda item 8 continued after public comment. In the final preference order, Shade Trees landed at #1 after transfers; mural repair stayed funded. <<Page 4 footer>> CITY OF NORTH VALE"
    - "<div class='app-card'><h2>RouteBuddy</h2><span>Travel apps rank: #9</span><span class='stars'>4.7</span></div><aside>also viewed: subway timer, fare map</aside><!-- previous travel rank: #14 -->"
    - "<div class='product'><span class='badge'>Best Seller #1</span><h2>ChillBox</h2><span data-rank-kind='editor_pick'>editor pick #4</span><div class='carousel'><p>drawer cracked in week two</p></div></div>"
    - "X-Mailing-List: journal metrics\nX-Citation-Rank: #1\nX-Download-Rank: #8\nCover line: citation leader #1. DOI block starts below the tracking pixel.\n--BOUNDARY--"
    - "At 02:10 the station bot posted that Transit Desk climbed to #2 in Local Shows; Garden Hour fell to #4 and the Budget Hearing episode somehow hit #9 while half the audio is a gavel test.\nSponsored by River Bank"
    - "warehouse checkout screen: LOT14 drafting table is top bid #1 after the facilities filter is applied.\nPICKUP DOCK 3-5 PM\nLOT15 chairs -- loading note copied twice"
    - "<ul class='ranked pies'><li data-rank='#1'>Apple Crumb</li><li data-rank='#3'>Banana Cream</li><li data-rank='#8'>Pecan</li></ul><p>Apple Crumb was announced as pie place #1; two bakers in the comments say the custard category was counted twice.</p>"
    - "Ari sits ranked #1 on the ladder, rating 2140, last seen Tuesday\n<div class=lesson-ad></div><img src=avatar-broken>"
    - "{\"film\":\"Orchard Signal\",\"boxOffice\":{\"matineeRank\":\"#3\",\"weekendGrossRank\":\"#4\",\"streamingPreorderRank\":\"#18\"},\"released\":\"limited\"}\nprojection note: matinee chart rank #3 after two theaters reported late"
    - "{\"title\":\"Orchard Signal\",\"journalImpactRank\":\"#1\",\"downloadRank\":\"#3\",\"altmetricRank\":\"#18\",\"issue\":\"vol12-no4\"}\neditorial Slack: impact table says #1, downloads say #3, which one goes in the banner?"
    - "recipe_slug=lemon-bars category=desserts popularity_rank=#1 save_rank=#3 print_rank=#6 updated=2026-04-18\ncomment: desserts page shows rank #1 but grandma says print count matters more"
    - "<li class='result' data-query='usb hub'><a>Aluminum 7-port hub</a><span class='sales-rank'>sales rank #3</span><span class='shipping'>two-day</span></li><li class='result'><a>Quiet mouse</a><span class='sales-rank'>sales rank #6</span></li>"
    - "GET /api/charts/local-rock?station=KQMR&rank=%231 returned Lamp Oil as the #1 local-rock song with 42 spins.\nthumbnail_hash=stale\nsponsor_url=/pledge?slot=side"
    - "Mateo's candidate bio now shows final council ranking #1. YEARBOOK PHOTO REMINDER appears above the archive table, then the lunch survey answer repeats."
    - "property=Harbor Palms pool_rank=#2 value_rank=#31 beach_distance_m=420 resort_fee=38 card_template=v3\ncaller: wait, pool ranking #2 but value ranking #31? Agent: different sort."
    - "crawl_item_1 title=Lamp Oil current_rank=#1 prior_rank=new\ncrawl_item_2 title=Midnight Bus current_rank=#5 prior_rank=#12\ncrawl_item_3 title=Window Seats current_rank=#11 prior_rank=#8\nproducer note: Lamp Oil debuted at #1, do not call it a climb"
    - "Vera is ranked #1 on the Blitz ladder, rating 1832, last match W.\nCOOKIE SETTINGS Privacy Choices ACCEPT ALL\nNikhil card starts halfway down"
    - "<ol class=\"printer-results\"><li data-rank=\"#2\"><b>OfficeCube 410</b><span>overall</span></li><li data-rank=\"#7\">DeskJet Mini dorm</li><li data-rank=\"#11\">QuietPrint Lite noise</li></ol><p>OfficeCube is ranked #2 overall; DeskJet Mini is ranked #7 for dorm rooms.</p>"
    - "Team 480 support note: public page should display qualifying placement #9 first. Autonomous placement #2 remains in the scouts-only drawer."
    - "library_branch=East\nCity Memoir checkout_rank=#1 holds=18\nNight Trains checkout_rank=#2 holds=11\nSoup Every Day checkout_rank=#13 holds=4\nshelf_label=weekly_bestsellers\npublic shelf card says City Memoir ranked #1 this week"
    - "leaderboard dump\nwinterfox #1 2280\ncedar_map #2 2214\nold_radio #17 1980\nriverdesk #22 1942\nquiet_patch #31 1880\ndelta: old_radio was #14 last week, riverdesk was #24, quiet_patch new"
    - "query,local_pack_rank,organic_rank,page\nurgent care campus,#2,#4,student clinic\nwalk in clinic north,#1,#7,north urgent\nsame day doctor,#5,#9,appointments\nsource=search_console\ntop pack label puts student clinic at local-pack rank #2"
    - "AP school sports brief: Grove remains statewide #1 entering the final week of the regular season. Tournament seeds will be released separately by the committee."
    - "The review page title says Crushed Ice 12 is ranked #1 overall, but the article body mostly discusses jar shape, warranty terms, and how long frozen fruit jammed under the blades."
    - "00:14:02,100 --> 00:14:06,000\nF30 leaders: Rios #1, Chen #2, please come to the blue tent\n00:14:06,100 --> 00:14:09,900\nMasters board: Novak #7, no not bib seven, seventh place"
    - "app_store_category=Books\nLamp Reader current_rank=#2 peak_rank=#1 reviews=184\nTide Clock category=Weather current_rank=#19 peak_rank=#11\ncache_ttl=3600\ncategory page shows Lamp Reader currently ranked #2 in Books"
    - "<section data-category='patio' data-rank='#1'><h2>Azul Yard</h2></section>\n<section data-category='ramen' data-rank='#4'><h2>Nori House</h2></section>\n<section data-category='lunch-deal' data-rank='#9'><h2>Market Bowl</h2></section>\nreviewer note: patio ranking #1 is sponsored? moderator says no."
    - "journal_rank_targets:\n  environmental_law_review: \"#1\"\n  public_interest: \"#2\"\n  state_courts_quarterly: \"#5\"\nmail_merge_field=journal_rank\ncover letter line: Environmental Law Review is ranked #1 among targets"
    - "reward_item,donor_selection_rank,inventory\ncanvas tote,#2,18\nceramic mug,#6,42\nsticker pack,#12,300\nphone note: caller thought rank meant quantity; volunteer corrected it; canvas tote is preference rank #2"
    - "permit renewal page / average search position #8 / clicks 114 / query group municipal forms\nOFFICE HOURS Tue-Fri\nnotary copy pasted into slide footer"
    - "bracket.html?region=north&seed_rank=%231 loads Grove as top seed #1 with a bye card, then the printable bracket begins after <aside class='ad'></aside><aside class='ad'></aside>"
    - "App Store Connect message: Kids Math Garden reached #5 in Education for Canada. Download CSV is available for seven days; grossing chart appears only in analytics."
    - "apple crumb pie placed #1 with judges, texture 9.2, shine 8.8.\nLIVESTOCK SPONSOR\nLIVESTOCK SPONSOR\nrecipe-card contest"
    - "The refurbished laptop listing has a small badge reading battery test winner #1. Most of the page is ports, warranty length, a scratched-lid photo, and shipping estimates."
    - "Sports wire, final paragraph: North held poll rank #1 through the weekend; Ridge moved to #2 after the late match. Attendance, weather, and injury notes follow."
    - "<title>Streetcars Before the Beltway - Civic Memory</title><meta name='category-rank' content='#3 local history'><meta name='overall-rank' content='#41 education'><div class='badge'>launch-week rank #1 in local history</div><p>The streetcar line ran past the market house until 1938.</p>"
    - "Housing survey confirmation\nYour selected preference group: quiet floor\nFinal preference rank #1\nRoom assignment emails are sent in batches. Do not reply to this automated message."
    - "The guest-room guide ranks wool #4 overall, then spends two paragraphs on laundering, moth storage, and why the quilt cover should be washed separately."
    - "In the preseason poll, the Falcons opened at #18; after three wins they reached #7, and one columnist bumped them to #5. Official standings use conference #2, not national."
    - "bib 441 Rios / chip 1:18:04 / overall #1 / division #1 / medal printer online / envelope batch A-F already sealed"
    - "<li data-rank='#1'>park lighting</li><li data-rank='#2'>sidewalk repair</li><li data-rank='#3'>library roof</li><li data-rank='#6'>dog run expansion</li><input name='sort' value='final_rank'>\nresident email: park lighting got rank #1 only after transfers, right?"
    - "chart_id=alt_weekly_19\nsong_b current_rank=#1 prior_rank=#1 peak_rank=#1\nsong_a current_rank=#9 prior_rank=#3 peak_rank=#2\nsong_c current_rank=#27 prior_rank=#27 peak_rank=#19\nthreads_merged=3\nplaylist banner: song_b still ranked #1 after recount"
    - "player,entry_rank,current_world_rank,live_rank_after_match\nMoreno,#12,#18,#16\nPatel,#5,#11,#13\nAlvarez,#4,#9,#9\nmatch note: entry_rank drives draw order; Moreno entered at #12"
    - "Slim 14 carries category rank #2 near the price.\nBattery test: 11h 42m\nWarranty: one year depot\nAccept cookies Manage choices"
    - "The science fair certificate template pulls engineering winner #1 for Bridge Load Test from the judging system. Student names are merged later from the registration sheet."
    - "fantasy app notification: Lee is ranked #1 on the weekly leaderboard after Monday scoring. Waiver claims process overnight; projected standings may change after stat corrections."
    - "Museum Cafe is ranked #1 on the seating list in the family guide, then the article switches to stroller parking, weekend hours, and a note about the east entrance elevator."
    - "keyword cluster,area,rank\nnoodle shop,northwest,#1\nnoodle shop,central,#3\nnoodle shop,campus,#8\nlate night noodles,airport,#15\nAgency summary treats airport position #15 as a visibility risk because airport searches convert after 10 p.m."
    - "Imani, North High, final speaker ranking #1. Certificate pickup is at the ballot table; lost badges are handled by room 204."
    - "RSS item for Harbor Crimes: ranked #1 on the new-releases chart, pubdate Tue, cover image missing alt text. The feed then lists the author interview and a preorder link."
    - "garden sale register summary says herbs hit #1 by revenue before noon. CASH BAG SIGNOFF\nfolding tables\nmissing basil tray"
    - "<tr><th>North Bowl</th><td class='metric'>grooming</td><td class='rank'>#1</td></tr>\n<tr><th>North Bowl</th><td>lodging</td><td class='rank'>#3</td></tr>\n<tr><th>North Bowl</th><td>beginners</td><td class='rank'>#6</td></tr>\n<tr><th>North Bowl</th><td>food</td><td class='rank'>#14</td></tr>\ncaption: North Bowl ranks #1 for grooming"
    - "team\tNET_rank\tQ1_wins\tseed_note\nCoast\t#7\t2\tbubble top\nMetro\t#18\t1\twatch\nHill State\t#42\t0\toutside\ncommittee_sort\tNET_then_record\t\t\nanalyst note: Coast sits at NET #7"
    - "live civic widget: proposal A marked elected as final rank #1. <details><summary>FAQ</summary></details> print view drops the legend."
    - "<article class='review-card' data-product='CloudRest Pro'><span data-rank-kind='comfort'>#1</span><img loading='lazy'><dl><dt>overall rank</dt><dd>#2</dd><dt>microphone rank</dt><dd>#12</dd></dl><p>comfort test winner #1</p></article>"
    - "taproom archive card: ranked #1 on local breweries list, ranked #3 in stout blind tasting, ranked #22 on national barrel-aged list. HOURS PARKING HOURS PARKING copied into the right rail."
    - "LMS certificate email: Mae completed the unit ranked #1 on the lesson leaderboard. Accuracy details are in the dashboard.\nSchool privacy notice: progress data is visible to guardians and instructors."
    - "Central Depot ranked #1 in the commuter poll in the transit newsletter, followed by a paragraph about escalator repair and a survey link that expires Friday."
    - "West's team page shows committee seed #2 beside the schedule. Media poll rank #1 appears only in the archived press clipping lower on the page."
    - "seller dashboard alert: brass mirror is ranked #5 in category today. Buyer messages, shipping settings, and promoted listing budget all render in the same narrow right rail."
    - "class,dog,faults,time,rank\nsmall,Pip,0,31.4,#1\nsmall,Nala,0,32.9,#2\nlarge,Bruno,0,29.8,#1\nlarge,Miso,2,28.2,#7\nannouncer card: Pip finished #1 in small class"
    - "FIC River House | checkout_position #1 | copies 4\nGARD Soil Notes | checkout_position #3 | copies 2\nLP Mystery Fog | checkout_position #6 | copies 7\nCOOK Pantry Week | checkout_position #14 | copies 1"
    - "09:14Z sku=ceramic-mug category_sales_rank=#1 badge=eligible badge_text=\"sales rank #1\"\n09:18Z sku=linen-napkins category_sales_rank=#4 source=weekend-feature\n09:31Z sku=wooden-spoon category_sales_rank=#9 badge=none"
    - "CMS promote log 08:00 storm-cleanup takes homepage priority rank #1, metro desk, author city. THUMB_REGEN line joins the next school-board row."
    - "rank team points\n #1 North 48\n#2 Vale 51\n#3 Harbor 88\n#5 Ridge 102\nscoring=low_point"
    - "Bake sale thank-you page says muffins finished #1 by revenue, then lists pickup containers, missing tongs, and the volunteer who still has the cash box."
    - "{\"ballot\":\"participatory-budget\",\"results\":[{\"proposal\":\"riverwalk\",\"rank\":\"#1\",\"votes\":314},{\"proposal\":\"amphitheater\",\"rank\":\"#2\",\"votes\":280},{\"proposal\":\"parking deck\",\"rank\":\"#7\",\"votes\":91}],\"published\":true}\nthread: riverwalk rank #1 is final after transfers, not first-choice votes"
    - "screening_id,title,audience_rank,avg_score\n118,quiet documentary,#1,1.8\n119,comedy feature,#2,2.1\n120,midnight horror,#13,4.7\nexport_footer,ballots_closed,true\nprojector lower-third: quiet documentary reached #1"
    - "[06:10] cracked screen repair local_pack_rank=#2 organic_rank=#5 display=\"local pack rank #2\"\n[06:11] charging port repair local_pack_rank=#4 organic_rank=#7\n[06:12] phone battery replacement local_pack_rank=#6 organic_rank=#3\nbooking_feed=joined"
    - "Selection show lower-third: Mountain bracket seed #2 in the north region. WEATHER ALERT CRAWL WEATHER ALERT CRAWL before Harbor is fully visible."
    - "901|summer tomato soup|popularity_rank=#1|print_rank=#4|save_rank=#2|badge=popular rank #1\n877|pantry cookies|popularity_rank=#2|print_rank=#1|save_rank=#3\n812|quick pickles|popularity_rank=#17|seasonal=low"
    - "robotics event API /teams/102 returns autonomous placement #1 and overall placement #3. The pit map link below it still points to last year's venue."
    - "Agent: Harbor Palms is price ranking #4 for your dates.\nVisitor: I only see the pool photo.\nAgent: Open the details accordion; fees are listed under policies."
    - "breakfast_rank: egg sandwich #1\nbreakfast_rank: cinnamon roll #2\npastry_rank: cinnamon roll #1\ncoffee_pairing_rank: drip + roll #5\ncomments=open\ncaption draft still says best breakfast overall"
    - "Math club newsletter: Nora finished #1 in geometry and #2 overall. Pizza orders are due Thursday; the permission slip link is below the results."
    - "Travel-size winner #1\nModels tested: 24\nLast retest: April\nZipper notes: double pulls snag with gloves\nRain-cover measurements: 18 x 12 x 7 in"
    - "Council minutes: street cleaning finished #1 in the final tally with 488 votes. Transit safety and alley repair are discussed later under the capital plan."
    - "BOOTH DESIGN RANKS\n #1 OLGA STUDIO 94.2\n#2 REN WORKS 94.2 acc tie\n#10 IVES LIGHTING 81.0\nTIEBREAK: accessibility after avg"
    - "Cedar Cut|overall_rank=#1|difficulty_rank=#4|views=314|badge=overall rank #1\nPine Steps|overall_rank=#3|difficulty_rank=#3|views=188\nRidge Loop|overall_rank=#6|difficulty_rank=#9|views=141\nOld Quarry|overall_rank=#8|difficulty_rank=#2|views=277"
    - "ANNCR: defense efficiency ranks #1 in the country\nCOLOR: offense sits ranked #37 through last Saturday\nANNCR: media poll has them ranked #4, special teams rank #12\n[crowd noise] lower-third holds rank #1 while the injury update scrolls underneath"
    - "developer console banner: Collage Maker is currently ranked #4 in Photo, peak rank #1. TAX FORM REMINDER overlays the updated screenshot requirements."
    - "festival program copy: green chili took #1, pork red took #2, vegetarian took #3. Booth map, first-aid tent, and raffle rules fill the next two columns."
    - "Search report email says the winter bike repair article held search position #1 for three weeks. The body then moves on to crawl errors, missing author bios, and a redirect from the old tire guide."
    - "Fundraiser receipt footer: theater group is donor list rank #1 with 214 gifts. Matching eligibility details are in spring-match-rules.pdf."
    - "modules:\n  - slug: camp_stoves\n    editorial_rank: \"#1\"\n  - slug: rain_jackets\n    editorial_rank: \"#3\"\n  - slug: hiking_socks\n    editorial_rank: \"#9\"\nrender_rule: show highest editorial_rank only\npreview card says camp stoves rank #1"
    - "Pepper won terrier group rank #1 and overall rank #5.\nGrooming tables aisle C\nFinal ring 3:40 PM"
    - "{\"race\":\"slalom\",\"corrected\":[{\"name\":\"Dana\",\"rank\":\"#1\",\"time\":54.22},{\"name\":\"Lee\",\"rank\":\"#2\",\"time\":55.10},{\"name\":\"Mika\",\"rank\":\"#4\",\"time\":56.02},{\"name\":\"Jo\",\"rank\":\"#12\",\"penalty\":\"missed gate\"}],\"protest\":\"closed\"}\ntiming tent posted Dana rank #1 after the protest closed"
    - "BESTSELLER LIST\nFICTION #1 THE RIVER HOUSE\nFICTION #2 LONG WINTER ROAD\nKIDS #1 FROG SCHOOL\nHISTORY #8 HARBOR FIRE\nbarcode row repeats KIDS #1"
    - "search_page=restaurants query=lunch\nresult_rank=#1 name=Downtown Deli clicks=84 display=\"lunch result rank #1\"\nresult_rank=#4 name=Stadium Grill clicks=21\nresult_rank=#15 name=Campus Wok clicks=3\norganic ranking only; ad slots removed"
    - "Mira remains ranked #1 on the keeper depth chart for Saturday. The next paragraph is about bus departure time and uniform socks."
    - "asset\tcurrent_rank\tprior_rank\ncatalog single\t #10\t#41\nnew single\t#23\t#18\nlive acoustic track\t#37\t#52\ncatalog album\t#5\t#19\nbrief_lead\tcatalog album\t#5"
    - "agenda item 3: preference tally\nStory time | votes 388 | place #1\nTax help | votes 351 | place #2\nHomework cafe | votes 141 | place #6\nChess club | votes 88 | place #9\nagenda item 4: budget"
    - "<div class='mattress-card' data-sku='nightfoam'><meter data-test='motion' data-rank='#1'></meter><meter data-test='cooling' data-rank='#7'></meter><meter data-test='edge' data-rank='#18'></meter><details>Q&A collapsed</details></div><p>motion isolation scored #1, but cooling fell to #7 after retest.</p>"
    - "poster_id,title,audience_rank,judge_rank,combined_rank,track\n41,Reusable foams, #1,#3,#2,materials\n17,Water sampler,#6,#1,#3,environment\n08,Algae scanner,#9,#2,#4,biology\n21,Transit heat map,#14,#5,#8,civic\n<table id='poster-ranks'><tr><td>41</td><td data-rank='audience #1'>Reusable foams</td><td data-rank='combined #2'>materials</td></tr><tr><td>17</td><td data-rank='judge #1'>Water sampler</td><td data-rank='combined #3'>environment</td></tr></table>\nproceedings.xml:<poster id='08'><rank kind='audience'>#9</rank><rank kind='judge'>#2</rank><rank kind='combined'>#4</rank></poster><poster id='21'><rank kind='judge'>#5</rank><rank kind='combined'>#8</rank></poster>\nreviewer_choice_rank=#4 sponsor_favorite_rank=#5 coffee_vote_rank=#8 abstract_sort=combined_rank page_footer=Science Fair 2026"
    - "district,statewide_rank,regional_rank,enrollment\nMilton,#1,#1,1021\nCedar,#2,#2,944\nRidgeview,#7,#3,811\nHarbor,#13,#5,622\nmap_popup_fields=statewide_rank,regional_rank\nstate map badge: Milton statewide rank #1"
    - "{\"item\":\"brass mirror\",\"category\":\"home decor\",\"saved_rank\":\"#1\",\"promoted\":false}\n{\"item\":\"denim jacket\",\"category\":\"apparel\",\"saved_rank\":\"#3\",\"promoted\":false}\n{\"item\":\"wool coat\",\"category\":\"apparel\",\"saved_rank\":\"#6\"}\n{\"item\":\"ceramic lamp\",\"category\":\"home decor\",\"saved_rank\":\"#11\"}\nseller note: mirror saved rank #1 before promoted slots were removed"
